# M5StopWatch FMO 接收端设计文档

> 版本：1.2
> 版本：1.2
> 日期：2026-08-11
> 日期：2026-08-11
> 项目：fwmo_main（M5StopWatch FMO 通联伴侣）
> 项目：fwmo_main（M5StopWatch FMO 通联伴侣）

## 1. 系统架构

### 1.1 总体拓扑（同一 WiFi 局域网）

```
┌────────────────── WiFi 路由器 ──────────────────┐
│                                                  │
│   ┌──── FMO 盒子 (WiFi 设备) ────┐               │
│   │ 192.168.3.172               │               │
│   │ · WebSocket 服务器          │               │
│   │ · AIN+/NET PTT 模拟接口     │               │
│   └───────┬─────────────────────┘               │
│           │ WebSocket (ws://192.168.3.172)       │
│   ┌───────▼─────────────────────┐   ┌──────────┐│
│   │      M5StopWatch            │   │ ESP32     ││
│   │  · 接收：WebSocket          │   │ 发射端    ││
│   │  · 发射：UDP 麦克风         │──▶│ (UDP→DAC) ││
│   └─────────────────────────────┘   └────┬─────┘│
│                                          │ 模拟音频
│                                   ┌──────▼─────┐│
│                                   │ FMO 盒子    ││
│                                   │ AIN+/NET PTT││
│                                   └─────────────┘│
└──────────────────────────────────────────────────┘
```

### 1.2 双向通联分工

| 方向 | 路径 | 说明 |
|------|------|------|
| **📥 接收** | FMO盒子 → WebSocket → StopWatch | 音频流 + 呼号/台站/事件（JSON）|
| **📤 发射** | StopWatch → UDP → ESP32 → 模拟音频/PTT → FMO盒子 | 麦克风语音上行 |

> **设计原则**：接收必须走 WebSocket——StopWatch 需要解析呼号、台站信息等 JSON 广播数据；发射走 ESP32 模拟桥接——因为 FMO 盒子的发射接口是模拟音频（AIN）+ PTT 电平，StopWatch 无法直接接线。

## 2. 接收端（StopWatch）设计

### 2.1 WebSocket 连接架构

FMO 盒子提供 3 个 WebSocket 端点（`fwmo_client.h`）：

| 端点 | 协议 | 数据 | 用途 |
|------|------|------|------|
| `ws://<host>/audio` | WebSocket Binary | 8kHz PCM 16bit 单声道 | 音频流 |
| `ws://<host>/events` | WebSocket Text | JSON 通联事件 | 呼号/说话状态/QSO |
| `ws://<host>/ws` | WebSocket Text | JSON 中继台列表 | 台站选择 |

### 2.2 线程模型

```
main loop()  ──▶ WebSocket I/O（非阻塞轮询）
     │
     ├── handleAudio()    → 音频帧 → fwmo_audio.pushPCM()
     ├── handleEvents()   → JSON → 队列投递
     ├── handleStation()  → JSON → 队列投递
     └── handleRequests() → 请求响应

FreeRTOS 任务: json_parse（core 0, 优先级4）
     │
     ├── parseEventJson()  → 呼号/说话状态/缓存记录
     └── parseStationJson() → 台站列表
```

### 2.3 连接管理

**连接建立**（`connectAll()`）：
- 3 条连接（audio/events/station）并行建立
- 断线自动重连，**指数退避**：3s → 6s → 12s → 24s → 30s 封顶

**音频连接独立重连**：
- 音频失败走独立退避（3s → 30s）
- 避免与事件连接相互阻塞

### 2.4 音频接收（handleAudio）

```cpp
uint8_t buf[4096];          // 读缓冲
while (available && 30ms时限 && <8帧) {
    len = wsRead(audio_ws, buf, sizeof(buf));
    if (len >= 2)
        audio->pushPCM((const int16_t*)buf, len/2);   // 本地播放
    if (蓝牙已连)
        bt->writePCM(buf, len, 8000);                 // 蓝牙转发（可选）
}
```

- **每 30ms 处理一批**（防阻塞主循环）
- **最多 8 帧/批**（控制单次处理量）
- 帧格式：**8kHz PCM 16bit 单声道**（`fwmo_settings.h` 中确认）

### 2.5 音频输出模式（fwmo_audio.h）

```
enum OutputMode { MODE_LOCAL=0, MODE_BT=1, MODE_AUTO=2 };
```

- **MODE_LOCAL**：扬声器直接播放（无蓝牙时）
- **MODE_BT**：蓝牙转发（有蓝牙连接时）
- **MODE_AUTO**：自动选择（默认）
- `localActive()`：当前是否本地播放（`MODE_LOCAL` 或 AUTO 且无蓝牙）

### 2.6 事件解析（parseEventJson）

```json
// 呼号事件 type=qso, subType=callsign
{
  "type": "qso",
  "subType": "callsign",
  "data": {
    "callsign": "BD6JNF",
    "isSpeaking": true
  }
}
```

**处理逻辑**：
1. `_talker_callsign` ← 对方呼号（当前通联者）
2. `_speaking` ← 是否在说话
3. 说话时：振动提醒 + 缓存记录（`fwmo_cache.h`）

**台站列表**（parseStationJson）：
- 解析中继台列表 → `_pinned_list`
- UI 台站选择页显示

### 2.7 UI 展示（fwmo_ui_lvgl.h）

| UI 元素 | 数据源 | 说明 |
|---------|--------|------|
| 呼号 `_call` | `currentTalker()` + `isSpeaking()` | 正在通联才显示，否则 NOCALL |
| 台站 `_ctxt` | `stationName()` | 当前台站名，无则"请稍后..." |
| 模式 `_mode` | `isSpeaking()` | ON AIR / Standby |
| 台站列表 | `pinnedList()` | 中文字体渲染 |

## 3. 发射端（ESP32 桥接）设计

### 3.1 发送链路（StopWatch 侧）

```
M5.Mic 采集 (16kHz/16bit) → UDP 打包 → WiFi 发送
```

**UDP 包格式**（`fwmo_mic_wifi.h`）：
```
[0:2]  序号 (uint16 big-endian)
[2:1024] PCM 数据 (511×16bit)
每包 1024 字节 ≈ 32ms 音频
目标: <FMO_AUDIO_RX_IP>:12345
```

### 3.2 触发逻辑（fwmo_main.ino）

```
A键按住 ≥150ms → WiFi已连接 → micWifi.setTransmit(true)
松开 A 键       → micWifi.setTransmit(false)
WiFi 未连接     → 屏幕提示 "WiFi 未连接"
```

> 蓝牙语音方案已作废（fwmo_mic.h 删除），蓝牙串口保留用于数据透传。

### 3.3 接收桥接（ESP32 侧，fmo_tx_esp32.ino）

```
UDP 接收 1024B 包 → 拉高 GPIO4 (NET PTT) → I2S DAC 输出 → 模拟音频
超时200ms无包 → 拉低 PTT
```

**发射端有两种硬件方案**：

#### 方案 A：ESP32 经典版（内置 DAC，GPIO25）

```
UDP 接收 1024B 包 → 拉高 GPIO4 (NET PTT) → 内置DAC(GPIO25) → 模拟音频
超时200ms无包 → 拉低 PTT
```

**模拟输出**：
- GPIO25（DAC1）→ [10kΩ] → FMO AIN+
- [10kΩ] 节点 → [2.2kΩ] → GND（分压 ≈ 5.5:1，内置 DAC ~1Vpp → ~180mVpp ✓）
- GPIO4 → FMO NET PTT（高=发射）

**代码要点**：`I2S_MODE_DAC_BUILT_IN` + `i2s_set_dac_mode(I2S_DAC_CHANNEL_LEFT_EN)` + `dac_output_enable`

#### 方案 B：ESP32-S3 + PCM5102A（外接 I2S DAC，推荐）

> ⚠ **ESP32-S3 没有内置 DAC**，必须外接 PCM5102A（标准 I2S DAC）。

```
UDP 接收 1024B 包 → 拉高 GPIO4 (NET PTT) → I2S TX → PCM5102A → 模拟音频
超时200ms无包 → 拉低 PTT
```

**PCM5102A 接线**：

| PCM5102A | ESP32-S3 | 说明 |
|----------|----------|------|
| VIN | 3.3V | 供电 |
| GND | GND | 共地（同时接 FMO GND）|
| BCK | GPIO5 | I2S 位时钟 |
| LRC | GPIO6 | I2S 帧时钟 |
| DIN | GPIO7 | I2S 数据 |
| FMT | GND | **标准 I2S 格式（必须）** |
| SCK | GND | 内部生成 MCLK |
| XSMT | 3.3V | **解除静音（必须，否则无声）** |
| LCK | GND | 只用左声道 |
| OUTL | 分压网络 | → FMO AIN+ |

**分压网络**（PCM5102A 输出 ~2.8Vpp，比内置 DAC 大）：

> ⚠ **音质关键**：PCM5102A 输出带 **~2.1V 直流偏置**，直接分压进 FMO AIN 会有直流成分导致削波失真。
> 必须**先串隔直电容**，再用**电位器**调电平（比固定电阻更可靠，能现场找到无失真且音量足够的最佳点）。

**电位器 3 引脚接法**（一个 100kΩ 电位器即可，本身是可变分压器）：

```
PCM5102A OUTL → [1µF隔直电容] → 电位器①脚 ┐
                                          ├─ 电位器②脚(W,滑动端) → FMO AIN+
                                     GND ←┘ 电位器③脚 → GND
```

| 电位器引脚 | 连接 |
|-----------|------|
| **① 脚（一端）** | 隔直电容后的音频信号输入 |
| **② 脚（W 滑动端）** | **输出 → FMO AIN+** |
| **③ 脚（另一端）** | GND |

> 旋钮调节时：②脚输出 = 输入 × 0%~100% 连续可调。
> 调试：讲话时旋转，使 FMO AIN 电平 ≤200mV 且无削波失真、音量足够即为最佳点。

- **隔直电容（1-10µF 无极性）**：滤掉 2.1V 直流偏置，只留音频交流成分
- **电位器 100kΩ**：现场调节输出电平

> 若不用电位器，可用固定电阻分压：**47kΩ:4.7kΩ**（衰减 11 倍 → ~254mVpp）
> 偏大改用 **100kΩ:4.7kΩ**（衰减 22 倍 → ~127mVpp）

**代码要点**（与方案 A 的区别）：
- 移除 `driver/dac.h`、`I2S_MODE_DAC_BUILT_IN`、`i2s_set_dac_mode`
- 改用 `I2S_MODE_TX` + `I2S_COMM_FORMAT_STAND_I2S` + `i2s_set_pin()`
- 引脚：`I2S_BCK=5, I2S_WS=6, I2S_DOUT=7`

### 3.4 关键参数一致性

| 参数 | StopWatch 发送端 | ESP32 发射端 |
|------|-----------------|-------------|
| 采样率 | 16kHz | 16kHz |
| 位深 | 16bit | 16bit |
| UDP 端口 | 12345 | 12345 |
| 包大小 | 1024 字节 | 1024 字节 |

## 4. 配置项

| 配置 | 位置 | 值 | 说明 |
|------|------|-----|------|
| `FMO_AUDIO_RX_IP` | fwmo_config.h | 192.168.x.x | **ESP32 发射端 IP** |
| `FMO_AUDIO_RX_PORT` | fwmo_config.h | 12345 | UDP 端口 |
| `DEFAULT_FMO_HOST` | fwmo_config.h | 192.168.3.172 | FMO 盒子 IP（WebSocket）|
| `WIFI_SSID/PASS` | fmo_tx_esp32.ino | 用户 WiFi | ESP32 联网 |

## 5. 文件清单

| 文件 | 作用 |
|------|------|
| `fwmo_client.h` | FMO WebSocket 客户端（接收核心）|
| `fwmo_audio.h` | 音频播放（ES8311 扬声器）|
| `fwmo_mic_wifi.h` | 麦克风 → UDP 发送（发射）|
| `fwmo_ui_lvgl.h` | UI（呼号/台站/模式显示）|
| `fwmo_settings.h` | 配置管理（host/IP）|
| `fmo_tx_esp32/fmo_tx_esp32.ino` | ESP32 桥接发射端（独立程序）|

## 6. 注意事项

1. **接收/发射独立**：接收走 WebSocket（TCP 可靠），发射走 UDP（实时低延迟），互不干扰
2. **音频格式差异**：接收 8kHz（盒子下发），发射 16kHz（麦克风采集），各自独立处理
3. **PTT 时序**：ESP32 收到包即拉高 PTT，200ms 无包释放——避免持续占网
4. **分压电路必需**：FMO AIN 输入 ≤200mV
   - 经典版内置 DAC（~1Vpp）：10kΩ:2.2kΩ（衰减 5.5 倍）
   - S3 + PCM5102A（~2.8Vpp）：**先串 1µF 隔直电容**（滤 2.1V 直流偏置），再接 100kΩ 电位器（①输入/②输出到 AIN+/③接地）调电平；固定值用 47kΩ:4.7kΩ（衰减 11 倍），偏大改 100kΩ:4.7kΩ
5. **UDP 丢包**：局域网内丢包率低（<0.1%），若 WiFi 弱会偶发"咔哒"，可后续加丢包补偿（丢包用前包静音填充）
6. **蓝牙语音已移除**：`fwmo_mic.h` 已删除，A 键只走 WiFi UDP
