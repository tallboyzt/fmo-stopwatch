# M5StopWatch FMO 通联伴侣 — 开发修改记录文档

> 项目路径：`e:\张涛\m5stopwatch-fmo\`
> 主程序：`fwmo_main\fwmo_main.ino`
> 本文档记录从对话开始到当前的全部功能修改与演进过程。

---

## 一、项目概述

M5StopWatch C152（ESP32-S3 + 1.54" 466×466 AMOLED + LVGL v8.4）的 **FMO 通联伴侣** 固件。
核心功能：
- FMO 通联（WebSocket 接收音频/呼号/台站信息）
- 中文字体显示（呼号、台站、城市地址）
- 离线呼号→城市查询
- WiFi 记忆与自动重连
- **语音发射**（麦克风 → ESP-NOW → 接收端 → FMO）
- 息屏省电 + 摩斯码息屏动画

---

## 二、硬件关键信息（M5StopWatch C152）

| 硬件 | 详情 |
|------|------|
| 主控 | ESP32-S3，8MB PSRAM |
| 屏幕 | 1.54" AMOLED 466×466 |
| 音频芯片 | **ES8311 Codec**（ADC+DAC 一体）|
| 扬声器 | I2S_NUM_0，MCK=18/BCK=17/WS=15，**DOUT=21** |
| 麦克风 | I2S_NUM_1，时钟引脚同 18/17/15，**DIN=16** |
| PA 使能 | M5IOE1_G10（IO 扩展器引脚 9）|
| 音频电源 | M5IOE1_G3（IO 扩展器引脚 2）|
| ES8311 I2C | 地址 0x18，内部 I2C（SDA=47/SCL=48）|
| 按键 | A=GPIO2（黄）、B=GPIO1（蓝），低电平有效 |
| 电源键 | M5PM1 PMIC 硬件管理（按1s关机/4s断电，软件不可拦截）|
| 电池 | M5PM1 I2C 读取，450mAh，3300mV→0% / 4200mV→100% |

**⚠ 关键约束**：Mic(I2S1) 与 Speaker(I2S0) **共用时钟引脚 18/17/15**——这是多次音频冲突问题的根源。

---

## 三、功能修改与演进

### 1. 中文字体支持（早期已完成）

- **字库**：思源黑体（Source Han Sans CN Bold）24px，4bpp 抗锯齿
- **字符集**：ASCII + GB2312 一级汉字（3755）+ 生僻字补充
- **存储**：LittleFS（ffat 分区 0x610000，9.9MB），运行时加载到 PSRAM
- **格式**：自定义 FMOF 二进制格式（44字节头 + bitmap + glyph_dsc + unicode_list）
- **生成**：`font_gen.py` + `fs_pack.py` 打包烧录
- **LVGL 适配**：`LV_FONT_FMT_TXT_LARGE=1`（glyph_dsc 16字节，bitmap_index 20bit）

**本对话新增**：补充 16 个城市生僻字（亳衢泸漯濮滕盱眙醴邳邡禺莞坜坻猗）——解决城市名显示方块问题。字库总字符 3853 → **3869**。

### 2. 呼号 → 城市离线查询

- **数据源**：`huhao.csv`（用户提供，16479条呼号）+ `city_map_list.csv`（拼音→中文）
- **离线表**：`callloc.bin`（CLOC 格式，FNV-1a 哈希 + 城市表）
- **查询类**：`fwmo_callloc.h`（PSRAM 加载 + 哈希查表）
- **生成脚本**：`gen_callloc.py` + `city_prov.py`（353 城市→省份精确对照）

**显示规则**（用户最终确认）：
| 呼号类型 | 查库结果 | 显示 |
|---------|---------|------|
| 大陆 | 命中 | **中国+省+城市**（如"中国河南安阳"）|
| 大陆 | 未命中 | 中国+分区主省（如"中国湖北"）|
| 香港 | - | 中国香港 |
| 澳门 | - | 中国澳门 |
| 台湾 | 命中 | **中国台湾+城市** |
| 台湾 | 未命中 | 中国台湾 |

**本对话修改**：
- 台湾呼号（BV）强制"台湾+城市"前缀（修正表内错误省份数据）
- 查表命中统一加"中国"前缀
- 字库补充 16 生僻字

### 3. CallSignSearch 呼号查询页面

- 设置菜单新增 "CallSignSearch" 项
- 九宫格键盘输入呼号 → 查询 → 切到 **Search Result 页**
- 修复：`endInput()` 里 INP_LOOKUP 用 `return` 跳过末尾通用返回（原 bug：页面被覆盖回设置菜单）

### 4. WiFi 功能增强

#### 4.1 WiFi 记忆库（自动保存已连 WiFi）
- `FMO_Settings` 复用 `profiles[1..4]` 作为记忆槽
- `saveWifiCredential()` / `findWifiCredential()` 等方法
- 连接成功自动保存；断开后每 5 秒轮询记忆库自动重连

#### 4.2 开机扫描已存 WiFi
- setup 时同步扫描周围网络
- 匹配顺序：active_profile → 记忆库 → 兜底
- 自动连接匹配到的已保存 WiFi

#### 4.3 AudioTX IP 设置项
- 设置菜单新增 "AudioTX IP"（第 6 项）
- 运行时修改发射目标 IP（存 NVS），**不用重新编译刷固件**
- `SETTINGS_VERSION` 升 v2

#### 4.4 UDP 广播自动发现（已实现后被 ESP-NOW 取代）
- 接收端每 2 秒广播 "FMO-RX" 到 24680 端口
- StopWatch 监听自动发现 IP（10 秒有效期）

### 5. 语音发射方案演进（核心）

**需求**：StopWatch 麦克风 → 无线 → ESP32 → FMO AIN+ 发射到网络。

#### 5.1 方案演进过程

| 方案 | 传输 | 问题 | 状态 |
|------|------|------|------|
| WiFi UDP | UDP 12345，16bit | 需路由器、IP 配置 | 备用 |
| ESP-NOW（初版）| 8bit 压缩点对点 | I2S 冲突崩溃 | 演进 |
| **全双工 I2S（当前）** | **单 I2S0 TX+RX 并行** | ✅ 已解决 | **当前方案** |

#### 5.2 反复崩溃的根因（重要经验）

```
M5StopWatch 音频硬件：Mic(I2S1) 与 Speaker(I2S0) 共用时钟引脚 18/17/15
M5Unified 分开管理 → 半双工切换时：
  ① M5.Speaker.end() 停 MCLK → M5.Mic.begin() 写 codec I2C 无 ACK → 卡死
  ② playTask 与 Mic 竞争 I2S → 崩溃
  ③ M5.Mic.end() 内部等 task 退出 → 死锁
  ④ eTaskGetState(失效句柄) → assert 崩溃
```

#### 5.3 最终方案：全双工 I2S（参考 xiaozhi-esp32）

**核心**：单个 I2S_NUM_0 创建 TX+RX 双通道（ES8311 全双工 ADC+DAC）

```
ESP32-S3 I2S_NUM_0（全双工）
  ├── TX 通道 → DOUT=21 → ES8311 DAC → PA → 扬声器
  └── RX 通道 ← DIN=16 ← ES8311 ADC ← 麦克风
      同一组时钟 18/17/15，只由一个 I2S 外设驱动 → 无冲突！
```

**新模块 `fwmo_audio_full.h`**（参考 xiaozhi `CreateDuplexChannels`）：
- `CreateDuplexChannels()`：单 I2S0 建 TX+RX，`i2s_channel_init_std_mode` 双通道
- `CodecInit()`：ES8311 寄存器序列（DAC+ADC 合并，0x01=0xB5 时钟）
- `writeTx()` / `readRx()`：全双工读写
- `setPa()`：PA 控制（M5IOE1_G10）

**关键修改**：
- `M5.begin()` 禁用内置 Speaker/Mic（`internal_spk=false; internal_mic=false`）→ 避免占用 I2S0
- `fwmo_audio.h` 播放任务改用 `audioFull.writeTx()`（8kHz→16kHz 重采样）
- `fwmo_mic_espnow.h` 采集改用 `audioFull.readRx()`
- A 键逻辑：**PA 静音 + 发送标志**（全双工并行，无需切换）

**数据包协议**（ESP-NOW，≤250字节）：
```
[PTT标志 1B][序号 2B][8bit音频 240样本=15ms@16kHz]  = 243字节
```

**接收端**：`fmo_tx_espnow/fmo_tx_espnow.ino`（ESP-NOW 接收 → PCM5102A → FMO）

### 6. 音频 I2S 冲突修复经验（排查记录）

| 症状 | 根因 | 修复 |
|------|------|------|
| 按 A 卡死（页面停留）| M5.Mic.begin() 与 Speaker 时钟冲突 | 全双工方案彻底解决 |
| 启动崩溃 `i2s legacy CONFLICT` | legacy i2s_driver_install 与 new API 冲突 | 移除 legacy，用 M5.Mic/new API |
| 按 A 重启 `assert eTaskGetState` | 任务自删后句柄失效 | 改用 `_task != nullptr` 检查 |
| 音频不能播放 | M5.begin() 占用 I2S0 | 禁用 internal_spk/mic |
| PA 默认关闭无声 | 未调 setPa(true) | begin() 里默认开 PA |

### 7. 日期时间显示优化

- 未同步（1970年）：显示占位符 `--.--.--` / `--:--`
- 同步后：`%Y-%m-%d` / `%H:%M`
- 用 `_lastDate[0]` 判断状态变化避免每帧重绘

### 8. 息屏省电 + 摩斯码动画

**功能**：
- **A+B 长按 3 秒** → 息屏（省电模式）
- 息屏：黑色覆盖层 + **呼号摩斯码绿色圆点闪烁**
- **任意 A/B 键** → 唤醒恢复

**交互逻辑**（短按/长按区分）：
| 操作 | 行为 |
|------|------|
| A+B 短按（<3s）| 打开/关闭菜单 |
| **A+B 长按（≥3s）**| **息屏**（不触发菜单）|
| 息屏时 A/B 任意键 | 唤醒 |
| A+B 关菜单后 | 300ms 抑制 A 键 TX（防误触发送）|

**摩斯码实现**（`fwmo_ui_lvgl.h`）：
- `_morseCode()`：A-Z/0-9 摩斯码表
- `_buildMorse()`：呼号 → 亮灭序列（点=1tick 亮 120ms，划=3tick，符号间隔1，字符间隔3，呼号间隔7）
- 每次息屏**动态读取** `owner_callsign`（用户填写的 MyCallSign）→ 解析为摩斯码
- 绿色圆点（14×14）中央闪烁，120ms/tick

**省电**：
- `display->setBrightness(12)` 低亮度（呼吸点可见）
- 息屏时跳过 `drawAll()`（UI 刷新），但保留 `lv_timer_handler()`（驱动摩斯动画）
- WiFi 保持连接，音频播放进程继续运行

---

## 四、文件修改清单

### 新增文件
| 文件 | 说明 |
|------|------|
| `fwmo_main/fwmo_audio_full.h` | 全双工音频（单 I2S0 TX+RX，参考 xiaozhi）|
| `fwmo_main/fwmo_mic_espnow.h` | 麦克风→ESP-NOW 发送（全双工 RX 采集）|
| `fwmo_main/fwmo_callloc.h` | 呼号→城市离线查询 |
| `fmo_tx_espnow/fmo_tx_espnow.ino` | ESP-NOW 接收端（PCM5102A → FMO）|
| `udp_audio_test.py` | 电脑端 UDP 音频测试脚本 |
| `fwmo_main/gen_callloc.py` | 呼号表生成脚本 |
| `fwmo_main/city_prov.py` | 城市→省份对照 |
| `fwmo_main/font_gen.py` / `fs_pack.py` | 字库生成/烧录 |

### 修改文件
| 文件 | 主要改动 |
|------|---------|
| `fwmo_main/fwmo_main.ino` | A 键全双工逻辑、息屏/唤醒、WiFi 扫描、按键短长按区分 |
| `fwmo_main/fwmo_ui_lvgl.h` | 息屏摩斯动画、Search Result 页、AudioTX IP 项、菜单居中 |
| `fwmo_main/fwmo_audio.h` | 播放改用全双工 TX、移除 M5.Speaker |
| `fwmo_main/fwmo_settings.h` | audio_tx_ip 字段、WiFi 记忆库方法、版本 v2 |
| `fwmo_main/fwmo_wifi.h` | connectedPass()、扫描匹配 |
| `fwmo_main/fwmo_config.h` | FMO_AUDIO_RX_IP/PORT、DISCOVER_PORT、BAT 参数 |
| `fwmo_main/fwmo_battery.h` | M5PM1 I2C 读取（450mAh 校准）|

### 删除文件
| 文件 | 原因 |
|------|------|
| `fwmo_main/fwmo_ui.h` | 旧版废弃 UI（IDE 缓存残留需清理）|

### 参考项目
| 项目 | 用途 |
|------|------|
| `demo/` | 官方固件（电池/显示参考）|
| `esp32-audio-communication-main/` | ESP-NOW 音频参考（STA 模式共存）|
| `xiaozhi-esp32-main/` | **ES8311 全双工 I2S 参考（当前方案来源）**|

---

## 五、关键技术要点

### 1. 全双工 I2S（xiaozhi 方案核心）
```cpp
i2s_new_channel(&chan_cfg, &tx_handle_, &rx_handle_);  // 单 I2S 建 TX+RX
i2s_channel_init_std_mode(tx_handle_, &std_cfg);        // 同一配置
i2s_channel_init_std_mode(rx_handle_, &std_cfg);
i2s_channel_enable(tx_handle_);
i2s_channel_enable(rx_handle_);                          // 全双工
```

### 2. FreeRTOS 任务安全（避免崩溃）
- 任务自删前**先置 `_task=nullptr`**
- 停止等待**只用 `_task != nullptr` 判断**（不用 `eTaskGetState`——失效句柄会 assert）

### 3. M5Unified 音频管理注意
- `M5.begin()` 默认启用 Speaker/Mic → 占用 I2S0 → 必须禁用（`internal_spk=false`）
- 播放任务在 `_paused` 时跳过 playRaw（不碰 I2S）

### 4. 呼号摩斯码（息屏）
- 点 = 1tick(120ms) 亮，划 = 3tick 亮
- 符号间隔 1、字符间隔 3、呼号循环间隔 7 tick
- 每次息屏动态读取 MyCallSign

---

## 六、当前架构总览

```
M5StopWatch (fwmo_main)
├── WebSocket ← FMO 服务器（音频/呼号/台站）
├── I2S_NUM_0 全双工
│   ├── TX → ES8311 DAC → PA(G10) → 扬声器（WS 音频 16kHz）
│   └── RX ← ES8311 ADC ← 麦克风（ESP-NOW 发送）
├── ESP-NOW → 接收端 ESP32 → PCM5102A → FMO AIN+（语音发射）
├── WiFi 记忆库（自动重连/开机扫描）
└── 息屏省电（A+B 长按 3s + 摩斯码动画）

接收端 (fmo_tx_espnow.ino)
└── ESP-NOW 接收 → 8bit→16bit → I2S → PCM5102A → FMO
```

---

## 七、使用说明

### 语音发射
1. 烧录 `fmo_tx_espnow.ino` 到接收端 ESP32，看串口打印 MAC
2. 把 MAC 填入 `fwmo_mic_espnow.h` 的 `FMO_RX_MAC`
3. 按住 A 键 → 语音发送（PA 静音，本地无声）
4. 松开 A → 恢复

### 息屏/唤醒
- 长按 **A+B 3秒** → 息屏（绿色圆点按 MyCallSign 摩斯码闪烁）
- 任意 **A/B 键** → 唤醒

### 呼号查询
设置 → CallSignSearch → 输入呼号 → Search Result 页显示"中国XX"

### 摩斯码息屏动画
1. 设置 → MyCallSign → 填写呼号（如 BD6JNF）
2. 长按 A+B 息屏 → 绿色圆点按呼号摩斯码闪烁（每次息屏动态读取）

---

*文档整理日期：2026-08-13*
